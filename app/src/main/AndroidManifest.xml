<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- android13的图片、音频、视频的权限 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" /> <!-- 存储权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" /> <!-- 如果不加这一行会出现警告 -->
    <!-- 打开相机权限 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" /> <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".inital.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".tonguedetection.UserInfo1"
            android:exported="false" />
        <activity
            android:name=".tonguedetection.UserInfo0"
            android:exported="false" />
        <activity
            android:name=".takephoto.Camera2ApiActivity"
            android:exported="false" />
        <activity
            android:name=".tonguereport.UserInfo_photo1"
            android:exported="false" />
        <activity
            android:name=".tonguereport.PhotoGalleryActivity"
            android:exported="false" />
        <activity
            android:name=".tonguereport.External_PhotoGalleryActivity"
            android:exported="false" />
        <activity
            android:name=".tonguereport.DisplayPhotoActivity"
            android:exported="false" />
        <activity android:name=".takephoto.photo_album"
            android:exported="false"/>

    </application>

</manifest>